using Microsoft.AspNetCore.SignalR;

namespace ConoPizzaWeb.Hubs
{
    public class OrdersHub : Hub
    {
        // You can leave it empty for now, or add server-side methods if needed
        // for example, public async Task Something(...) { ... }
    }
}
